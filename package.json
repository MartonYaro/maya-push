{
  "name": "maya-push",
  "version": "0.1.0",
  "private": true,
  "description": "MAYA Push — deps declared at root so Nixpacks detects the native better-sqlite3 dependency and provisions the build toolchain. The server lives in /server and resolves these via Node's upward module lookup.",
  "scripts": {
    "start": "node server/src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "google-play-scraper": "^10.0.1",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^3.0.3"
  }
}
